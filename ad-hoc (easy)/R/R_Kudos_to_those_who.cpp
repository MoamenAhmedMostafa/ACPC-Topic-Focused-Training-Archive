#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
int main()
{
    ios_base::sync_with_stdio(false);cin.tie(nullptr);
    int tt;
    cin>>tt;
    while(tt--)
    {
        unsigned ll n;cin>>n;
        cout<<n<<endl;
        /*
        why only n 
        the summution of first n odd numbers in n*n  and the summution of first n even number is n * (n+1)
        so we need to calculate gcd ( n*n , n*(n+1) )
        which is n  
        */
    }

    return 0;
}

// Failure is another stepping stone to greatness.
// It's fine to celebrate success, but it is more important to heed the lessons of failure.
// Competition is the fuel that ignites innovation.